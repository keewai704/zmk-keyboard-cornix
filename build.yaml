# This file generates the GitHub Actions matrix
# For simple board + shield combinations, add them
# to the top level board and shield arrays, for more
# control, add individual board + shield combinations to
# the `include` property, e.g:
#
# board: [ "nice_nano_v2" ]
# shield: [ "corne_left", "corne_right" ]
# include:
#   - board: bdn9_rev2
#   - board: nice_nano_v2
#     shield: reviung41
#
# ZMK Studio is enabled on central builds only.
---
include:
  # Dongle is the split central in dongle mode. Studio is enabled on the
  # central only; the RPC snippet also keeps all Studio-assignable behaviors.
  - board: nice_nano//zmk
    shield: cornix_dongle_adapter cornix_dongle_eyelash dongle_display
    snippet: studio-rpc-usb-uart nrf52840-nosd
    cmake-args: -DCONFIG_ZMK_STUDIO=y
    artifact-name: cornix_dongle_nosd

  # Standalone/default split mode: left half is the central. Enable the same
  # Studio USB RPC transport here so Studio works over both USB and BLE.
  - board: cornix_left//zmk
    snippet: studio-rpc-usb-uart nrf52840-nosd
    cmake-args: -DCONFIG_ZMK_STUDIO=y
    # shield: cornix_indicator
    artifact-name: cornix_left_default_nosd

  # Peripheral halves do not run Studio themselves.
  - board: cornix_ph_left//zmk
    # shield: cornix_indicator
    artifact-name: cornix_left_for_dongle_nosd

  - board: cornix_right//zmk
    # shield: cornix_indicator
    artifact-name: cornix_right_nosd

  - board: cornix_right//zmk
    shield: settings_reset
    snippet: studio-rpc-usb-uart nrf52840-nosd
    artifact-name: cornix_reset

  - board: nice_nano//zmk
    shield: settings_reset
    snippet: studio-rpc-usb-uart nrf52840-nosd
    artifact-name: reset_nicenano_nosd
